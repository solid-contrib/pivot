import { OidcHttpHandler, ProviderFactory } from '@solid/community-server';

/**
 * HTTP handler that redirects all requests to the OIDC library.
 */
export class PivotOidcHttpHandler extends OidcHttpHandler {
//   public constructor(providerFactory: ProviderFactory) {
//     super(providerFactory);
//   }
}
